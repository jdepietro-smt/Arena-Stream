// Class definitions live in encoder.h (inline / template).
// This TU exists so CMake has a compilation unit for the library target
// and for any future non-template helper implementations.
#include "encoder.h"
